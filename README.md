# 📸 Hasselblad Style Watermark Generator (哈苏风格水印生成器)

这是一个基于 Flask 和 Python Pillow 的 Web 应用程序，用于自动生成类似哈苏（Hasselblad）官方样张中的高级渐变水印。

它可以智能分析用户上传的摄影作品，提取画面中的环境色（Ambient Colors），通过独家的 **V5 终极调色算法**，生成高亮、鲜艳且通透的渐变文字水印。

## ✨ 核心特性

* **🎨 V5 终极调色算法**：
    * **高光锁定**：强制锁定左侧渐变为“淡金/米白”色，模拟高光质感。
    * **暴力提亮与鲜艳化**：彻底解决暗部显脏的问题。强制提升右侧暗部颜色的亮度和饱和度，确保生成的渐变色既浓郁又通透，拒绝沉闷。
    * **智能色相修正**：自动检测并修正“土黄”、“泥垢”等不讨喜的颜色，将其引导向高级的橙红/金黄色系。
* **🌥️ 投影分离技术 (Shadow Separation)**：
    * 在文字下方自动生成柔和的半透明投影，使文字与复杂背景有效分离，产生悬浮感，保证在任何图片上都清晰可见。
* **📐 双格式输出**：
    * 支持下载 **PNG** (带透明通道的高清位图)。
    * 支持下载 **SVG** (内嵌字体与渐变的无损矢量图)，适合大幅面打印或再次设计。
* **🐳 Docker 容器化**：
    * 开箱即用，支持多架构构建（自动适配 x86_64 和 Apple Silicon ARM64）。
    * 解决了 macOS AirPlay 端口冲突问题（默认运行在 5001 端口）。

## 📂 目录结构

```text
hasselblad-watermark/
├── app.py                  # Flask 后端核心逻辑 (V5 算法)
├── Dockerfile              # Docker 构建文件
├── requirements.txt        # Python 依赖
├── templates/
│   └── index.html          # 前端界面
├── static/                 # 存放生成的临时文件
└── fonts/
    └── font.ttf            # 【必须】字体文件 (需自行添加)
```

## 🚀 快速开始

### 前置要求

在运行之前，您**必须**准备一个字体文件：
1. 在项目根目录下创建一个名为 `fonts` 的文件夹。
2. 找一个粗体的无衬线字体（推荐 `Arial Black Italic` 或 `Source Han Sans Bold` 等支持中文的字体）。
3. 将字体文件放入 `fonts` 文件夹，并**重命名为 `font.ttf`**。

> **注意**：如果不放入字体文件，程序将无法启动或报错。

### 方式一：本地直接运行

1. **克隆仓库**
    ```bash
git clone https://github.com/Simontage/hasselblad-watermark.git
cd hasselblad-watermark
    ```

2. **安装依赖**
    建议使用虚拟环境：
    ```bash
python3 -m venv venv
source venv/bin/activate  # Windows 使用: venv\Scripts\activate
pip install -r requirements.txt
    ```

3. **运行应用**
    ```bash
python app.py
    ```

4. **访问**
    打开浏览器访问：[http://localhost:5001](http://localhost:5001)

### 方式二：使用 Docker (推荐)

无需配置 Python 环境，直接构建运行。

1. **构建镜像**
    ```bash
docker build -t hasselblad-watermark .
    ```

2. **运行容器**
    ```bash
docker run -d -p 5001:5001 --name my-watermark hasselblad-watermark
    ```

3. **访问**
    打开浏览器访问：[http://localhost:5001](http://localhost:5001)

## 🛠️ 技术栈

* **后端**: Flask
* **图像处理**: Pillow (PIL)
* **色彩算法**: RGB to HSV Conversion, Custom V5 Vivid Algorithm
* **前端**: HTML5, CSS3 (Dark Mode), Vanilla JS
* **部署**: Docker

## 📝 注意事项

* **端口说明**: 默认端口已配置为 `5001`，以避免与 macOS Monterey+ 默认开启的 AirPlay Receiver (占用 5000 端口) 发生冲突。
* **字体版权**: 请确保您使用的 `font.ttf` 拥有相应的商业授权，本项目不包含任何字体文件。
* **文件存储**: 生成的临时文件将保存在 `static/` 目录下，建议定期清理。
* **性能**: 对于高分辨率图片，处理时间可能会有所增加，请耐心等待。

## 🤝 贡献

欢迎提交 Issue 或 Pull Request 来改进算法！

## 📄 开源协议

MIT License
